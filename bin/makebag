#!/usr/bin/env ruby

require_relative '../lib/chipmunk_bagger'

unless ARGV.length == 4
  raise ArgumentError, "Usage: #{$0} content_type external_id /path/to/source_files /path/to/output_bag" 
end

ChipmunkBagger.new(*ARGV).make_bag
